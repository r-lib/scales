# A guide is a list of breaks, labels and values, along with the aesthetic
# that they apply to. The guide code in this package should make it easier to
# make legends in base, lattice and ggplot2 graphics.
#

make_legend <- function(name, breaks, labels, values) {
  
}
