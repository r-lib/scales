# Identity transformation (do nothing)

Identity transformation (do nothing)

## Usage

``` r
transform_identity()

identity_trans()
```

## Examples

``` r
plot(transform_identity(), xlim = c(-1, 1))
```
