\name{log_breaks}
\alias{log_breaks}
\title{Breaks on integers.}
\usage{log_breaks(n=5, base=10)}

\description{
  Breaks on integers.
}
\arguments{
  \item{n}{desired number of breaks}
}
\examples{log_breaks()(c(1, 1e6))
log_breaks()(c(1, 1e5))}
